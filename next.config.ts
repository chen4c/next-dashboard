import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Reduce the image size by using the standalone output mode
  output: 'standalone',
  /* config options here */
};

export default nextConfig;
